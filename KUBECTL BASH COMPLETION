Install Bash Completion:

sudo apt install bash-completion

kubectl completion bash > kubecom.sh

mv kubecom.sh .kube/

source $HOME/.kube/kubecom.sh

For every user:

vi .profile and paste source $HOME/.kube/kubecom.sh